---
import DemoNav from '../components/DemoNav.astro';
import '../styles/global.css';
import '../styles/visual-2026.css';

interface Props {
  title: string;
  eyebrow?: string;
  description?: string;
  trace?: string;
}

const {
  title,
  eyebrow = 'xedap.vn',
  description = '',
  trace = 'Trace: BA → DA → UI/UX',
} = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <title>{title} — SearchOps Raw UI</title>
  </head>
  <body>
    <div class="app-shell">
      <aside class="sidebar"><DemoNav /></aside>
      <section class="app-column">
        <header class="topbar">
          <div class="breadcrumbs">
            <span>Workspace</span><span>/</span><strong>{eyebrow}</strong>
          </div>
          <div class="top-actions">
            <button class="icon-button" aria-label="Search">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><circle cx="11" cy="11" r="6"/><path d="m16 16 4 4"/></svg>
            </button>
            <button class="icon-button" aria-label="Notifications">
              <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9"/><path d="M10 21h4"/></svg>
            </button>
            <div class="avatar">KT</div>
          </div>
        </header>

        <main class="page-shell">
          <header class="page-header">
            <div>
              <div class="page-eyebrow">{eyebrow}</div>
              <h1>{title}</h1>
              {description && <p>{description}</p>}
            </div>
            <div class="page-actions"><slot name="actions" /></div>
          </header>

          <div class="prototype-banner">
            <strong>Raw visual prototype</strong>
            <span>{trace}</span>
            <span class="prototype-divider"></span>
            <span>Dummy data follows product contracts; not production metrics.</span>
          </div>

          <slot />
        </main>
      </section>
    </div>
  </body>
</html>
